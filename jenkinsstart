pipeline {
    agent any
    stages {
           stage('Load') {
           code = load 'example.groovy'
           }
           stage('Execute') {
          code.example1()
          }
        }
    }
}
